import Box from "../weatherDisplay/Box";

const DayList = (props)=>{
  const data = props.weatherList;

  console.log(data)

  // data.map(obj=>{
  //   obj.map(data=>{

  //   });
  // });

  return <h1>hello</h1>

};

export default DayList;